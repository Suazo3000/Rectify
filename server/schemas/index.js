const typeDefs = require('./typeDefs'); // Import type defs
const resolvers = require('./resolvers'); // Import resolvers

module.exports = { typeDefs, resolvers }; // Export object containing type defs and resolvers
